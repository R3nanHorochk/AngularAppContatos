import { FiltroContatoPipe } from './filtro-contato.pipe';

describe('FiltroContatoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroContatoPipe();
    expect(pipe).toBeTruthy();
  });
});
